// filter selected elements based on a condition and return an array
// with the elements that fulfilled the condition

//amra akhon player variable theke je sob player 60 inches upore tader k return karbe

const numbers = [1, 3, 6, 7, 8, 15]
const player = [75, 65, 44, 76, 55, 59,]

const selected = player.filter(p => p > 60);
console.log(selected)